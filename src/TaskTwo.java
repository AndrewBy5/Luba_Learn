public class TaskTwo {
    /*public void playWithMisterX() {
        int misterX = -100;
        if (misterX > 0) {
            misterX++;
        } else if (misterX < 0) {
            misterX -= 2;
        } else if (misterX == 0) {
            misterX = 10;
        }
        System.out.println(misterX);
        System.out.println("________________________");
    }

    public void orderLinesOfNumbers() {
        int a = -125;
        int b = -250;
        int c = -1000000;
        int min = 0;
        int max = 0;
        int aver = 0;

        if (a > b && a > c) {
            max = a;
        } else if (b > a && b > c) {
            max = b;
        } else if (c > a && c > b) {
            max = c;
        }

        if (a < b && a < c) {
            min = a;
        } else if (b < a && b < c) {
            min = b;
        } else if (c < a && c < b) {
            min = c;
        }

        if (min < a && a < max) {
            aver = a;
        } else if (min < b && b < max) {
            aver = b;
        } else if (min < c && c < max) {
            aver = c;
        }

        System.out.println("В порядке убывания: " + max + "," + aver + "," + min);
        System.out.println("В порядке возрастания: " + min + "," + aver + "," + max);
        System.out.println("________________________");
    }


    public void averageOfNumbers() {
        int l = 0;
        int o = -20;
        int p = 5;
        int average = 0;


        if (o < l && l < p || p < l && l < o) {
            average = l;
        } else if (l < o && o < p || p < o && o < l) {
            average =o;
        } else if (l < p && p < o || o < p && p < l) {
            average = p;
        }
        System.out.println("Среднее число: " + average);
        System.out.println("________________________");
    }*/
}
